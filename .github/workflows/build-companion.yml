name: Build Android Companion APK

on:
  push:
    branches: [ main ]
    paths:
      - 'companion-android/**'
  pull_request:
    paths:
      - 'companion-android/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('companion-android/**/*.gradle*', 'companion-android/**/libs.versions.toml') }}
          restore-keys: gradle-

      - name: Build debug APK
        working-directory: companion-android
        run: gradle assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hub-companion-debug
          path: companion-android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
